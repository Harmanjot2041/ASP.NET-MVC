@model SignUp
@*
    For more information on enabling MVC for empty projects, visit http://go.microsoft.com/fwlink/?LinkID=397860
*@
@if (Model != null)
{
    @using (Html.BeginForm("EditEmployee", "Home", FormMethod.Post)) ;
    {
        @*<input type="hidden" id="hdnId" value="@Model.Id" />
        <div class="s1">
            <div>Id</div>
            @Html.TextBoxFor(m => m.Id)
        </div>
        <div class="s1">
            <div>Name</div>
            @Html.TextBoxFor(m => m.Name)
        </div>
        <div class="s1">
            <div>Age</div>
            @Html.TextBoxFor(m => m.Age)
        </div>
        <div class="s1">
            <div>Salary</div>
            @Html.TextBoxFor(m => m.Salary)
        </div>
        <div class="s1">
            <div>Designation</div>
            @Html.TextBoxFor(m => m.Desination)
        </div>
        <div>
            <input type="submit" value="Contact" />
        </div>
        <div>*@
                <form asp-controller="Home" asp-action="EditEmployee" method="post">
                    <div class="form-group">
                        <label>
                            Id
                        </label>
                        <input value="@Model.Id" type="text" placeholder="id" />

                    </div>
                    <div class="form-group">
                        <label>
                            Name
                        </label>
                        <input value="@Model.Name" type="text" placeholder="Name" />
                    </div>
                    <div class="form-group">
                        <label>
                            Age  
                        </label>
                        @Html.TextBoxFor(m => m.Age)
                        @Html.ValidationMessageFor(m => m.Age, "", new { @class = "text-danger" })
                    </div>
                    <div class="form-group">
                        <label>
                            Salary
                        </label>
                        <input value="@Model.Salary" type="text" placeholder="salary" />
                    </div>
                    <div class="form-group">
                        <label>
                            Designation
                        </label>
                        <input value="@Model.Desination" type="text" placeholder="designation" />
                    </div>
                    <button class="btn btn-outline-danger" type="submit">Submit</button>
                </form>
            
    }
}
<button class="btn btn-outline-warning"> <a asp-area="" asp-controller="Home" asp-action="EmployeeView"> Back </a></button>

